{
  "name": "icb-wayland",
  "private": true,
  "version": "1.0.0",
  "description": "Islamic Center of Boston (Wayland, MA) website. Pages are generated from content/ by build.js.",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check"
  }
}
